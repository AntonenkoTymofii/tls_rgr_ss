# РГР: Імітація TLS/SSL "Рукостискання" (Handshake)

Цей проєкт є реалізацією розрахунково-графічної роботи (розширена ЛР7).
Програма імітує процес встановлення захищеного з'єднання між клієнтом та 
сервером за протоколом, схожим на TLS 1.2/1.3.

## 📋 Реалізований функціонал

Виконано **Базовий** та **Розширений** варіанти завдання:

1.  **Асиметричне шифрування (RSA):** Використовується для безпечного 
обміну секретним ключем (`premaster secret`).

2.  **Симетричне шифрування (AES-GCM):** Використовується для шифрування 
повідомлень після встановлення з'єднання.

3.  **Генерація ключів (HKDF):** Використання `Client Random`, `Server Random` 
та `Premaster` для генерації унікального сеансового ключа.

4.  **Імітація мережі (Розширений варіант):**
    * Реалізовано клас `SimulatedNetwork`.
    * Дані розбиваються на пакети обмеженого розміру.
    * Додано штучну затримку (delay) для імітації "повільного" радіоканалу.

## 🛠 Технології

* **Мова:** Python 3.10
* **Бібліотека криптографії:** `cryptography` (стандарт індустрії для криптографічних примітивів).

## 🚀 Як запустити

1.  **Клонуйте репозиторій:**
    ```bash
    git clone [https://github.com/AntonenkoTymofii/tls_rgr_ss](https://github.com/AntonenkoTymofii/tls_rgr_ss)
    cd tls_rgr_ss
    ```

2.  **Створіть та активуйте віртуальне середовище:**
    ```bash
    python -m venv venv
    
    # Windows
    venv\Scripts\activate
    
    # macOS/Linux
    source venv/bin/activate
    ```

3.  **Встановіть залежності:**
    ```bash
    pip install cryptography
    ```

4.  **Запустіть симуляцію:**
    ```bash
    python main.py
    ```

## 📝 Приклад роботи

У консолі ви побачите покроковий лог:
* `ClientHello` / `ServerHello`
* Обмін сертифікатами (публічними ключами)
* Передачу зашифрованого `Premaster Secret`
* Повідомлення `Ready`
* Розшифровку фінального секретного повідомлення

---
**Автор:** Антоненко Тимофій Романович
**Група:** ІМ-21